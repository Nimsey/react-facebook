import styles from '../styles/globals.css';
import Head from 'next/head';
import { Lexend } from 'next-google-fonts'





export default function MyApp({ Component, pageProps }) {
    return <Component {...pageProps} />
}